[project]
name = "flowforge"
version = "0.1.0"
description = "A modular workflow automation platform built entirely in Python."
readme = "README.md"
requires-python = ">=3.12"

dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"